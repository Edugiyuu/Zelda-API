
import { Link } from 'react-router-dom';
import '../Styles/NavBar.css'
const NavBar = () => {
  return (
    <nav className='NavBar'>
      
          <Link to={`/`}>Home</Link>
          <Link to={`/`}>Home</Link>
          <Link to={`/`}>Home</Link>
          <Link to={`/`}>Home</Link>
          <Link to={`/`}>Home</Link>
          <Link to={`/`}>Home</Link>
          
          
        
    
    </nav>
  );
}

export default NavBar;